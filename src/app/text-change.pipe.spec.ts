import { TextChangePipe } from './text-change.pipe';

describe('TextChangePipe', () => {
  it('create an instance', () => {
    const pipe = new TextChangePipe();
    expect(pipe).toBeTruthy();
  });
});
